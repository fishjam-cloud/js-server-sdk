{
  "name": "selective-subscription-demo",
  "version": "0.25.0",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write .",
    "dev:frontend": "yarn workspace selective-subscription-frontend dev",
    "dev:backend": "yarn workspace selective-subscription-backend dev",
    "dev": "concurrently \"yarn dev:frontend\" \"yarn dev:backend\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "prettier": "^3.8.1",
    "typescript": "^5.9.3"
  }
}
