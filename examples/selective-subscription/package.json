{
  "name": "selective-subscription-demo",
  "version": "0.21.0",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write .",
    "typecheck": "bun run --filter '*' typecheck",
    "dev": "concurrently \"bun run --filter '*' dev\"",
    "build": "bun run --filter '*-frontend' build"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "prettier": "^3.6.2",
    "typescript": "^5.9.3"
  }
}
