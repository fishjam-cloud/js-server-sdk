{
  "private": true,
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "packageManager": "yarn@4.3.1",
  "devDependencies": {
    "typescript": "^5.4.5"
  },
  "scripts": {
    "build:sdk": "yarn workspace @fishjam-cloud/js-server-sdk build",
    "build:check": "yarn workspaces foreach -A -p run build:check"
  }
}
