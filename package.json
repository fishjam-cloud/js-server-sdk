{
  "private": true,
  "workspaces": [
    "packages/fishjam-openapi",
    "packages/fishjam-proto",
    "packages/js-server-sdk",
    "examples/*"
  ],
  "packageManager": "yarn@4.3.1",
  "devDependencies": {
    "typescript": "^5.4.5"
  },
  "scripts": {
    "build:check": "yarn workspaces foreach -A -p run build:check"
  }
}
