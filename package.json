{
  "private": true,
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "packageManager": "yarn@4.3.1",
  "devDependencies": {
    "typescript": "^5.4.5"
  },
  "scripts": {
    "build": "yarn workspace @fishjam-cloud/js-server-sdk build",
    "typecheck": "yarn workspaces foreach -A -p run typecheck"
  }
}
